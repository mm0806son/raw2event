opencv-python
easydict == 1.9
numpy == 1.26
tqdm == 4.49.0
torch
torchvision
scipy == 1.11.4
dv-processing
pandas
matplotlib
optuna
geomloss
pupil-apriltags
h5py
Pillow
spikingjelly
pyyaml

# Optional: only needed by tools/eqs/rvt_extractor.py, which loads the RVT
# backbone through hydra config composition.
# hydra-core
# omegaconf
